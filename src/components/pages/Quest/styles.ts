import styled from 'styled-components';
import { flex, spacings } from '../../../style/StyledComponents/variables';

export const StyledQuest = styled.div`
  ${flex('column')}; 
  padding-top: ${spacings.medium};
`;

export const Form = styled.form`
  ${flex('column')}
`;
