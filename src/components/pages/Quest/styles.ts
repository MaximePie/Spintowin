import styled from "styled-components";
import {flex} from "../../../style/StyledComponents/variables";

export const StyledQuest = styled.div`
  ${flex('column')};
`

export const Form = styled.form`
  ${flex('column')}
`