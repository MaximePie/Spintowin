
// @ts-ignore
// describe('Test is running in Card', () => {
//   it('Should run', () => {
//     expect(1).toBe(1);
//   })
// });

test('Test is running in Card', () => {
    expect(1).toBe(1);
});
